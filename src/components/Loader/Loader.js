import { Audio } from 'react-loader-spinner';

const Loader = () => (
  <Audio
    height="80"
    width="80"
    radius="9"
    color="green"
    ariaLabel="three-dots-loading"
    wrapperStyle
    wrapperClassName
  />
);

export default Loader;